public class informatics1_01 {
    public static void main(String[] args){

        //［問１］次の 10 進数を 2 進数に変換せよ．
        //（１）(0.25)10
        double n = 0.25;

        // 小数点の求め方わからん
        /* GPTのコード
        import java.util.Scanner;

public class DecimalToBinarySimple {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        System.out.print("10進数の小数を入力してください: ");
        double decimal = sc.nextDouble();  // 小数の入力

        System.out.print("2進数での表現: 0.");

        int precision = 10;  // 小数点以下10桁まで表示
        while (decimal > 0 && precision > 0) {
            decimal *= 2;
            if (decimal >= 1) {
                System.out.print(1);
                decimal -= 1;
            } else {
                System.out.print(0);
            }
            precision--;
        }

        System.out.println();  // 改行
        sc.close();
    }
}
         */



        //（２）(0.34)10

    }
}
